[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: BrowserRouter routes (e.g. /services, /admin) must serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
